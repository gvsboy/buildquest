/** @jsx React.DOM */
var React = require('react');

var AreasView = React.createClass({

    displayName: 'AreasView',

    render: function () {
        return (
            <div>Areas</div>
        );
    }
});

module.exports = AreasView;
